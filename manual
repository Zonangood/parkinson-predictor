List of Commands:
1. $python<version> -m venv venv. 2. $./venv/Scripts/activate or source venv/bin/activate
(replace to deactivate to exit from the environment)
3. $pip install -r requirements.txt
[ The requirement file should contain the above details before executing the
above command. ]
4. $uvicorn app:app --port 8000 --reload
[ This command is to run the FastAPI server. ]
